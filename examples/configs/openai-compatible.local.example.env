# Example OpenAI-compatible local model configuration.
# Copy this to your own local environment file if needed.
# Do not commit real credentials or private endpoints.

NEXUS_AGENT_BASE_URL=http://127.0.0.1:8080/v1
NEXUS_AGENT_MODEL=local-model
NEXUS_AGENT_API_KEY=not-required-for-local-example
